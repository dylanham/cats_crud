- @cats.each do |cat|
  %p= cat.name
  %p= cat.breed
  %p= cat.owner
  %p
    = link_to 'Edit', edit_cat_path(cat)
    = link_to 'Delete', cat_path(cat), method: :delete
= link_to 'New Cat', new_cat_path
